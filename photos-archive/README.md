# Archived Gallery Photos

These files were intentionally removed from the live gallery. They are kept
outside `photos/` so gallery validation and future maintenance cannot
accidentally publish them again.

Do not move a file back into `photos/` without also adding it to `gallery.json`
with a new permanent reference.
